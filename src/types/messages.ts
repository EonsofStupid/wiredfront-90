
/**
 * Re-export message types from schemas (type-only)
 */
export type { 
  Message, 
  MessageMetadata, 
  MessageRole, 
  MessageType, 
  MessageStatus,
  MessageRequest,
  MessageResponse
} from '@/schemas/messages';

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import React from "react";

export const GitHubRepositorySettings: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>GitHub Repository Settings</CardTitle>
      </CardHeader>
      <CardContent>
        {/* GitHub repository settings form will go here */}
      </CardContent>
    </Card>
  );
};

export default GitHubRepositorySettings;
